@using Moridge.Models
@model StatisticsSetModel
@{
    ViewBag.Title = "Statistik";
}
<link rel="stylesheet" type="text/css" href="~/Content/themes/moridge/moridge.style.css" />

@section Header {
    <a href="#nav-panel" id="nav-btn" data-icon="bars" data-transition="slideup" data-iconpos="notext">Menu</a>
    <h6 class="header-heading">
        @ViewBag.Title
    </h6>
}

<ul data-role="listview" data-inset="true" style="min-width: 210px;">
    @for (var i = 0; i < Model.StatisticsModels.Count; i++)
    {
        <li>
            <a href="@Url.Action("StatisticsDetails", "Admin", new { index = i })">
                <span>@Html.DisplayFor(m => m.StatisticsModels[i].CompanyName)</span>
                <span style="float: right">@Html.DisplayFor(m => m.StatisticsModels[i].BookingCount)</span>
            </a>
        </li>
    }
</ul>

@Html.ActionLink("Visa totalt", "StatisticsTotal", "Admin", new { @class = "ui-btn" })
