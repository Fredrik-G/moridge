@model Moridge.Models.StatisticsChart

@{
    var myChart = new Chart(width: 300, height: 450)
        .AddTitle(@Model.GetTitle())
        .AddSeries(
            name: "Employee",
            xValue: @Model.Dates,
            yValues: @Model.EventCount)
        .Write();
}